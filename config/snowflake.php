<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Snowflake Machine ID
    |--------------------------------------------------------------------------
    |
    | 雪花ID生成器的机器ID，范围：0-127
    | 在多节点部署时，每个节点必须配置不同的机器ID以确保ID唯一性
    |
    | 如果不配置，系统会尝试：
    | 1. 从环境变量 SNOWFLAKE_MACHINE_ID 读取
    | 2. 使用服务器IP地址的最后一段（取模）
    | 3. 使用随机数（不推荐，可能导致ID冲突）
    |
    | 注意：当前使用52位ID（41位时间戳 + 5位机器ID + 6位序列号）
    | - 支持32台机器（0-31）
    | - 支持约69年时间范围（从EPOCH开始）
    | - 完全兼容JavaScript安全整数范围（2^53 - 1），最大ID值约4.5千万亿
    |
    */

    'machine_id' => env('SNOWFLAKE_MACHINE_ID', null),

    /*
    |--------------------------------------------------------------------------
    | Epoch (时间戳起始点)
    |--------------------------------------------------------------------------
    |
    | 雪花ID的时间戳起始点（毫秒时间戳）
    | 默认：2024-01-01 00:00:00 UTC (1704067200000)
    |
    | 注意：
    | - EPOCH设置后不应随意更改，否则可能导致ID冲突
    | - 32位时间戳支持约136年时间范围（从EPOCH开始）
    | - 建议设置为项目启动时间或一个固定的历史时间点
    |
    */

    'epoch' => env('SNOWFLAKE_EPOCH', 1704067200000),
];
