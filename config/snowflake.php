<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Snowflake Machine ID
    |--------------------------------------------------------------------------
    |
    | 雪花ID生成器的机器ID，范围：0-127
    | 在多节点部署时，每个节点必须配置不同的机器ID以确保ID唯一性
    |
    | 如果不配置，系统会尝试：
    | 1. 从环境变量 SNOWFLAKE_MACHINE_ID 读取
    | 2. 使用服务器IP地址的最后一段（取模）
    | 3. 使用随机数（不推荐，可能导致ID冲突）
    |
    | 注意：当前使用45位ID（32位时间戳 + 7位机器ID + 8位序列号）
    | 以兼容JavaScript的安全整数范围（2^53），支持100台机器和60年时间范围
    |
    */

    'machine_id' => env('SNOWFLAKE_MACHINE_ID', null),

    /*
    |--------------------------------------------------------------------------
    | Epoch (时间戳起始点)
    |--------------------------------------------------------------------------
    |
    | 雪花ID的时间戳起始点（毫秒）
    | 默认：2024-01-01 00:00:00 UTC
    |
    */

    'epoch' => 1704067200000,
];
